development:
  # Configure available database sessions. (required)
  sessions:
    default:
      hosts:
        - ds061374.mongolab.com:61374
      database: memento_parking
      username: juanbausa
      password: wZUwu5XWi0Ax

test:
  sessions:
    default:
      database: memento_parking_test
      hosts:
        - localhost:27017
      options:
        read: primary
        # In the test environment we lower the retries and retry interval to
        # low amounts for fast failures.
        max_retries: 1
        retry_interval: 0
